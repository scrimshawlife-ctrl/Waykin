# Outdoor QA Receipts

Place filled copies of `OUTDOOR_QA_RECEIPT_TEMPLATE.md` here:

```text
OUTDOOR_QA_RECEIPT_YYYYMMDD_<device-model>.md
```

Empty until a human completes a physical walk. Do not invent OBSERVED results.
