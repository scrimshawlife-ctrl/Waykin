name: Waykin
options:
  bundleIdPrefix: com.waykin
  deploymentTarget:
    iOS: "17.0"
targets:
  WaykinApp:
    type: application
    platform: iOS
    sources:
      - App
      - Sources/WaykinCore
    settings:
      base:
        INFOPLIST_KEY_NSLocationWhenInUseUsageDescription: "Waykin uses your location during an active movement session to measure distance, pace, and route progress."
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: "YES"
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: "YES"
    dependencies:
      - package: WaykinCore
  WaykinTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
      - AppTests
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: WaykinApp
  WaykinUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - WaykinUITests
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: WaykinApp
packages:
  WaykinCore:
    path: .
    product: WaykinCore
schemes:
  Waykin:
    build:
      targets:
        WaykinApp: all
    test:
      targets:
        - WaykinTests
        - WaykinUITests
    run:
      config: Debug
